# EvaluAI Backend Requirements
# Instalar con: pip install -r requirements.txt

# Framework FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Base de datos
sqlalchemy==2.0.49
alembic==1.12.1
# PostgreSQL driver (container-friendly): psycopg v3 bundles needed libs without system libpq
psycopg[binary]==3.2.3

# Autenticación y seguridad
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==5.0.0
python-multipart==0.0.26
# Validación
# Railway/Railpack defaults to Python 3.13.x; older pydantic-core pins may not ship wheels
# and will try to compile from source (fails in slim builders).
pydantic==2.10.6
pydantic-core==2.27.2
pydantic-settings==2.6.1
email-validator==2.2.0

# Utilidades
python-dotenv==1.0.0
httpx==0.25.2

# LLM providers
groq==0.12.0

# Procesamiento de documentos
python-docx==1.1.0
PyPDF2==3.0.1

# Testing
pytest==8.3.5
pytest-asyncio==0.25.3