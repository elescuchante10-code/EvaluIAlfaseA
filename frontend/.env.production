# Build-time default for production builds (Vercel can override in Project → Environment Variables).
REACT_APP_API_URL=https://evaluadorib-production.up.railway.app
# Menos ruido de source maps en node_modules (html2pdf/docx-preview) en CI/Vercel
GENERATE_SOURCEMAP=false
