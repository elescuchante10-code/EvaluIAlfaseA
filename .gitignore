.cursor/
.vscode/

# Python
__pycache__/
*.py[cod]
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/
.env
.env.*
# Override local de Compose (no versionar)
docker-compose.local.yml
# Permitir versionar plantilla de variables (sin secretos reales)
!.env.example
# Excepción: solo URL pública del API (sin secretos); Vercel puede sobrescribir vía env del proyecto
!frontend/.env.production

# Repo temporal de referencia (no versionar)
.tmp-astra/

# Node / React
node_modules/
build/
dist/
.next/
.turbo/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Local databases / vector stores
*.db
*.sqlite3
chroma.sqlite3
chroma.sqlite3-*
*.bin

# App temporary artifacts
tmp_pdf_extract*
tmp_pdf_review*

# Teacher-context Karpathy: nuevos Markdown generados en local bajo md/
# (los .md ya versionados siguen en el índice; esto evita ruido ?? en git status)
backend/data/teacher_context/md/
# Teacher-context: datos locales por usuario (no versionar; puede contener material sensible/licenciado)
backend/data/teacher_context/users/

# Archives / exports
*.zip
